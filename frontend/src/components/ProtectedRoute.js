// src/components/ProtectedRoute.js
const ProtectedRoute = ({ children }) => {
    // Directly render children without checks for testing purposes
    return children;
  };
  
  export default ProtectedRoute;
  