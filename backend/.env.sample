# Database configuration
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=inventory_db

# Security keys (generate with: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=
JWT_SECRET_KEY=

# JWT settings (0 or empty for no expiration)
JWT_ACCESS_TOKEN_EXPIRES=0

# Flask configuration
FLASK_APP=run.py
FLASK_ENV=development
FLASK_DEBUG=1